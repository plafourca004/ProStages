{% extends 'base.html.twig' %}

{% block title %}Formation{% endblock %}

{% block body %}
<br>
<br>
    <h1>{{ formation.nom }}</h1>
<br>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formation.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ formation.nom }}</td>
            </tr>
            <tr>
                <th>Acronyme</th>
                <td>{{ formation.acronyme }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('formation_index') }}">Revenir à la liste</a>

    <a href="{{ path('formation_edit', {'id': formation.id}) }}">Modifier</a>

    {{ include('formation/_delete_form.html.twig') }}
{% endblock %}
